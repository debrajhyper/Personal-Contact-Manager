import axios from "axios";

// export default function authToken(token) {
//     const TOKEN_HEADER = 'Authorization';

//     if(token) {
//         axios.defaults.headers.common[TOKEN_HEADER] = `Bearer ${token}`;
//     } else {
//         delete axios.defaults.headers.common[TOKEN_HEADER];
//     }
// }